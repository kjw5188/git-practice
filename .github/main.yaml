name: Hello GitHub Actions 🚀

on:
  push:
    branches: [ "main" ]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Welcome Message 💬
        run: echo "Hello, World! GitHub Actions 작동 성공!"